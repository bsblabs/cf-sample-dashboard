spring.view.prefix=/WEB-INF/jsp/
spring.view.suffix=.jsp
server.port=${dashboard.controller.port}

oauth.info.uri=${uaa.url}/userinfo
oauth.token.check.uri=${uaa.url}/check_token
oauth.token.access.uri=${uaa.url}/oauth/token
oauth.logout.url=${uaa.url}/logout.do
oauth.authorization.uri=${uaa.url}/oauth/authorize

api.url=${api.url}

oauth.client.id=palmyra
oauth.client.secret=clientsecret

dashboard.suid.file=${dashboard.suid.file}